# Val-only hyperparameter search v2 (MPD276 match-val). Longer training + real early stop.
# Do NOT peek at MPD48 during selection.
epochs: 80
patience: 20
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/MPD_merged_pempni_val_sweep_v2'
gpus:
  - 0

ckpt: null
skip_test: true

run_name: 'mpd_pempni_val_hp2_'
wandb: false
num_folds: 1

accumulate_grad_batches: 8

checkpoint_monitor: val/all_pearson
checkpoint_mode: max
test_both_checkpoints: false
early_stop_monitor: val/all_pearson
early_stop_mode: max
