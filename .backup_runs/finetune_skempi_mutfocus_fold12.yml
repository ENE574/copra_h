# Same as finetune_skempi_mutfocus.yml but explicit fold_indices for partial reruns.
epochs: 30
patience: 50
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/SKEMPI_localdiff_cv'
gpus:
  - 3

ckpt: null

run_name: 'skempi_localdiff_'
wandb: false
num_folds: 5
fold_indices: [0, 1, 2, 3, 4]

accumulate_grad_batches: 4

checkpoint_monitor: val/all_pearson
checkpoint_mode: max
checkpoint_monitor_all: val/pc_pearson
checkpoint_mode_all: max
checkpoint_select: primary
test_both_checkpoints: true
early_stop_monitor: val/all_pearson
early_stop_mode: max
