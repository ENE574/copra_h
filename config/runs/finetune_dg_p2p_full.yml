# P2P dG fine-tuning - full data (V2020 + wt + mt) as train, no held-out test.
epochs: 50
patience: 50
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/finetune_dg_p2p_full'
gpus:
  - 7

ckpt: null

run_name: 'finetune_dg_p2p_full_'
wandb: false
num_folds: 1

accumulate_grad_batches: 4
checkpoint_monitor: train/all_rmse
checkpoint_mode: min
early_stop_monitor: train/all_rmse
early_stop_mode: min
skip_test: true
