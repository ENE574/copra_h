# Val-only hyperparameter search on MPD276 (match-val split). Do NOT peek at MPD48.
epochs: 30
patience: 150
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/MPD_merged_pempni_val_sweep'
gpus:
  - 3

ckpt: null
skip_test: true

run_name: 'mpd_pempni_val_hp_'
wandb: false
num_folds: 1

accumulate_grad_batches: 8

checkpoint_monitor: val/all_rmse
checkpoint_mode: min
test_both_checkpoints: false
early_stop_monitor: val/all_rmse
early_stop_mode: min
