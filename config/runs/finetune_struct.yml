epochs: 20
patience: 150
output_dir: '/media/SSD0/csd/lrg/outputs/PRA201_ablation'
gpus:
  - 3

ckpt: null

run_name: 'struc_off_'
wandb: False
num_folds: 5

# Gradient accumulation steps (effective batch size = batch_size * accumulate_grad_batches)
accumulate_grad_batches: 2