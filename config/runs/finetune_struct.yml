epochs: 50
patience: 150
output_dir: './outputs/PRA210_csan'
gpus:
  - 0

ckpt: null

run_name: 'ckpt=null_agb=1_lr=4e-5_wd=0_'
wandb: False
num_folds: 5

# Gradient accumulation steps (effective batch size = batch_size * accumulate_grad_batches)
accumulate_grad_batches: 1