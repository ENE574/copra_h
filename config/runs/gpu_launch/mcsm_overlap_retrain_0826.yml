# Re-train mCSM on original 90% overlap split (mCSM_overlap_90pct_v4.yml).
# Uses latest ddG model_config best_mcsm_B76_optC.yml.
epochs: 100
patience: 200
output_dir: /media/SSD0/csd/lrg/copra_h/outputs/mcsm_overlap_retrain_0826
gpus:
- 0
ckpt: null
skip_test: true
run_name: mcsm_overlap_retrain_0826_
wandb: false
num_folds: 1
accumulate_grad_batches: 4
checkpoint_monitor: val/all_rmse
checkpoint_mode: min
test_both_checkpoints: false
early_stop_monitor: val/all_rmse
early_stop_mode: min
