# MPD: 60% of val moved to train (train=318, val=20), GPU5, 0826 重训
# model: best_mpd.yml (physics_aux_max_weight=0.1, 保留可解释性)
epochs: 100
patience: 200
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/MPD_val60to_train_0826'
gpus: [0]
ckpt: null
skip_test: true
run_name: 'mpd_val60to_train_0826_'
wandb: false
num_folds: 1
accumulate_grad_batches: 4
checkpoint_monitor: val/all_rmse
checkpoint_mode: min
test_both_checkpoints: false
early_stop_monitor: val/all_rmse
early_stop_mode: min
