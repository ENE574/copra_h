# B10: mCSM train/val with PDB overlap, monitor val/all_pearson
epochs: 40
patience: 15
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/mCSM_rna_overlap'
gpus:
  - 3

ckpt: null
skip_test: true

run_name: 'mcsm_overlap_B10_'
wandb: false
num_folds: 1

accumulate_grad_batches: 8

checkpoint_monitor: val/all_rmse
checkpoint_mode: min
test_both_checkpoints: false
early_stop_monitor: val/all_rmse
early_stop_mode: min
