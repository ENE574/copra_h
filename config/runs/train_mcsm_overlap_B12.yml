# B12: complex_group fix + stronger ranking (weight=2, margin=0.5) + larger window + noise
epochs: 60
patience: 20
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/mCSM_rna_overlap'
gpus:
  - 3

ckpt: null
skip_test: true

run_name: 'mcsm_overlap_B12_'
wandb: false
num_folds: 1

accumulate_grad_batches: 8

checkpoint_monitor: val/all_rmse
checkpoint_mode: min
test_both_checkpoints: false
early_stop_monitor: val/all_rmse
early_stop_mode: min
