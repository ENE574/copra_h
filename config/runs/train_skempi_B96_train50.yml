# B96 recipe on SKEMPI, val 50% ALSO used as train (by PDB).
# val kept as val (full original val), extra duplicate rows marked 'train' added to train.
epochs: 100
patience: 200
output_dir: '/media/SSD0/csd/lrg/copra_h/outputs/SKEMPI_B96_train50'
gpus: [0]
ckpt: null
skip_test: true
run_name: 'skempi_B96_train50_'
wandb: false
num_folds: 5
accumulate_grad_batches: 4
checkpoint_monitor: 'val/all_rmse'
checkpoint_mode: 'min'
test_both_checkpoints: false
early_stop_monitor: 'val/all_rmse'
early_stop_mode: 'min'
